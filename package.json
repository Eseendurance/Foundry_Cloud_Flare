{
  "name": "foundry-cloud",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix platform",
    "engine": "npx tsx bin/engine.ts",
    "build": "npm run build --prefix platform"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}
